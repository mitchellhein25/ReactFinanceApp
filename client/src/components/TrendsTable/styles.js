import { makeStyles} from '@material-ui/core/styles';

export default makeStyles(() => ({
    head: {
        backgroundColor: 'lightgray',
      },
    tableHeader: {
        textAlign: "center",
        padding: "5px"
    }
}));