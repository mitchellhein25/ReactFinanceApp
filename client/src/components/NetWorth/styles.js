import { makeStyles} from '@material-ui/core/styles';

export default makeStyles((theme) => ({
    yellow: {
        backgroundColor: "gold",
        padding: '2px',
        paddingLeft: "5px",
        paddingRight: "5px",
        fontWeight: "500"
    },
}));